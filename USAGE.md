# LLMChat 使用指南

---

## 目录

- [首次启动配置](#首次启动配置)
- [界面概览](#界面概览)
- [聊天功能](#聊天功能)
- [发送图片与文件](#发送图片与文件)
- [会话管理](#会话管理)
- [切换 AI 服务商](#切换-ai-服务商)
- [设置页详解](#设置页详解)
  - [Claude 设置](#claude-设置)
  - [OpenAI 设置](#openai-设置)
  - [通用设置](#通用设置)
- [明/暗主题切换](#明暗主题切换)
- [ASCII Art 工具页](#ascii-art-工具页)
- [Analytics 会话分析页](#analytics-会话分析页)
- [Compare 模型对比页](#compare-模型对比页)
- [立绘表情联动](#立绘表情联动)
- [TTS 语音朗读](#tts-语音朗读)
- [背景图自定义](#背景图自定义)
- [系统托盘](#系统托盘)
- [快捷键](#快捷键)
- [数据存储与备份](#数据存储与备份)
- [常见问题](#常见问题)

---

## 首次启动配置

首次启动 LLMChat 后，需要先配置 API 密钥才能开始聊天：

1. 点击左侧导航栏中的 **Settings** 展开设置分组
2. 进入 **Claude** 或 **OpenAI** 设置页
3. 填写你的 **API Key**
4. 根据需要修改 **Base URL**（如果使用代理或中转地址）
5. 选择你想使用的 **Model**（模型）
6. 返回 **Chat** 页面即可开始对话

所有设置修改后会自动保存，无需手动点击保存按钮。

---

## 界面概览

LLMChat 采用 FluentUI 风格的现代化界面，整体结构如下：

```
┌──────────────────────────────────────────────────────┐
│  顶部标题栏（无边框美化窗口）                           │
├───────────┬──────────────────────────────────────────┤
│  左侧导航  │          右侧内容区                       │
│           │                                          │
│  ▸ Chat   │   根据导航选择显示对应页面：                │
│           │   - Chat 页面（聊天主界面）                 │
│  ▾ Settings│  - Claude 设置页                         │
│    Claude  │   - OpenAI 设置页                        │
│    OpenAI  │   - General 通用设置页                    │
│    General │                                          │
│           │                                          │
└───────────┴──────────────────────────────────────────┘
```

- **Chat** — 聊天主页面
- **Settings** — 可展开的设置分组，包含 Claude、OpenAI、General 三个子页面

---

## 聊天功能

Chat 页面是应用的核心交互界面，采用左右分栏布局：

```
┌──────────────┬──────────────────────────────────────┐
│  左侧面板     │          右侧聊天区                    │
│              │                                        │
│ [Claude ▾]   │   ┌─ Assistant ────────────────────┐   │
│ [+ New Chat] │   │ AI 回复支持 **Markdown** 渲染    │   │
│              │   │ - 代码块、粗体、列表、链接等      │   │
│ ┌──────────┐ │   └────────────────────────────────┘   │
│ │ 会话 1   │ │                                        │
│ │ 会话 2   │ │           ┌──── You ──────────────┐   │
│ │ 会话 3   │ │           │ 帮我写一首诗            │   │
│ └──────────┘ │           └───────────────────────┘   │
│              │                                        │
│              │   状态: ~125 tokens | 3.2s              │
│              │   [Translate] [Summarize] [Explain]     │
│              │   [附件预览区]                           │
│              │  [+] [ 输入消息...          ] [ Send ]  │
└──────────────┴──────────────────────────────────────┘
```

### 发送消息

1. 在底部输入框中输入消息（输入框高度随内容自适应，最高 120px）
2. 点击 **Send** 按钮或按 **Enter** 键发送（**Shift+Enter** 换行）
3. 发送后输入框会暂时禁用，状态栏显示 "Thinking..."
4. AI 的回复会以流式方式逐字显示（纯文本格式）
5. 回复完成后状态栏短暂显示估算 token 数和耗时（3 秒后消失）
6. 回复过程中按 **Esc** 可中止回复（已接收的部分内容会保留）

### Prompt 快捷模板

输入框上方有一排预设的 Prompt 标签按钮：

> Translate to English | Translate to Chinese | Summarize | Explain Code | Fix Bug | Optimize Code

点击任意标签，模板文本会自动填入输入框，可直接发送或在后面补充内容。

### 消息右键菜单

右键点击任意消息气泡会弹出菜单：

| 选项 | 说明 | 适用角色 |
|------|------|---------|
| **Copy** | 复制选中文本（无选中时复制全部内容） | 所有 |
| **Select All** | 全选消息文本 | 所有 |
| **Edit** | 将消息内容回填到输入框，删除该消息及之后的所有消息，修改后可重新发送 | 仅用户 |
| **Delete** | 撤回该消息及之后的所有消息 | 仅用户 |
| **Favorite / Unfavorite** | 收藏/取消收藏该消息（收藏后气泡带黄色边框） | 所有 |
| **Convert to ASCII** | 图片附件转为 ASCII 字符画，复制到剪贴板 | 含图片附件的消息 |

> Edit 和 Delete 操作会自动保存，不可撤销。

### 消息搜索

按 `Ctrl+F` 打开搜索栏，输入关键词可在当前会话中搜索消息，匹配的气泡会显示黄色边框，第一个匹配自动滚动到视图中。

### 字体缩放

聊天区域支持字体大小调节（范围 10px ~ 28px）：

| 操作 | 效果 |
|------|------|
| `Ctrl + 滚轮上` / `Ctrl + =` | 放大字体 |
| `Ctrl + 滚轮下` / `Ctrl + -` | 缩小字体 |
| `Ctrl + 0` | 恢复默认大小 |

缩放会同时应用到消息文本和角色名称标签。

---

## 发送图片与文件

LLMChat 支持向 AI 发送图片和文件进行多模态推理。

### 支持的文件类型

| 类型 | 格式 | 处理方式 |
|------|------|---------|
| **图片** | PNG、JPG、GIF、WebP、BMP | 以 base64 编码发送给 API（超过 2048px 自动缩放） |
| **文档** | PDF | Claude 原生支持；OpenAI 以文本提示附加 |
| **文本文件** | TXT、PY、CPP、JS、JSON、MD 等 | 读取内容作为文本发送 |

### 三种添加方式

1. **点击按钮**：点击输入框左侧的 **+** 按钮，弹出文件选择对话框
2. **拖拽文件**：将文件直接拖拽到聊天页面
3. **粘贴图片**：复制图片后在输入框中 **Ctrl+V** 粘贴（支持截图粘贴）

### 附件预览

添加的附件会在输入框上方显示预览条：
- 图片显示缩略图 + 文件名
- 文件显示文件名
- 点击每个附件右侧的 **x** 可以移除

附件可以和文字一起发送，也可以只发送附件（不输入文字）。

---

## 会话管理

LLMChat 支持多会话管理，每个会话独立保存聊天记录。

### 新建会话

点击左侧面板的 **+ New Chat** 按钮创建新会话。新会话会自动成为当前活跃会话。

### 切换会话

在左侧会话列表中点击任意会话即可切换。切换后右侧聊天区会加载该会话的历史消息。

### 重命名会话

右键点击会话列表中的某个会话，选择 **Rename**，在弹出的对话框中输入新名称。

### 导出会话

右键点击会话列表中的某个会话，选择 **Export**，选择保存路径即可导出为 Markdown（.md）文件。

### 移动到文件夹

右键点击会话，选择 **Move to Folder...**，输入文件夹名（留空为默认文件夹）。用于对会话分组管理。

### 自动生成标题

首轮对话完成后，系统会自动调用当前 Provider 生成一个简短的会话标题（替代默认的消息前 30 字标题）。

### 删除会话

右键点击会话列表中的某个会话，选择 **Delete**。删除后自动切换到其他可用会话。

> **注意**：在 AI 正在回复（流式响应中）时，无法进行新建、切换或删除会话的操作。

### 会话排序与持久化

- 发送消息后，当前会话自动移到列表顶部（最近活跃优先）
- 所有会话自动保存为 JSON 文件，下次启动时自动恢复

---

## 切换 AI 服务商

在 Chat 页面左侧面板顶部有一个下拉选择框，可以在 **Claude** 和 **OpenAI** 之间一键切换：

1. 点击下拉框选择目标服务商
2. 应用会自动重建 API 连接
3. 选择会被持久化，下次启动时自动恢复

切换服务商不会影响已有的聊天记录。

---

## 设置页详解

### Claude 设置

进入 **Settings → Claude** 页面配置 Claude API：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| **API Key** | Anthropic 的 API 密钥（以密码模式显示） | 无 |
| **Base URL** | API 基础地址，支持自定义代理/中转 | `https://api.anthropic.com` |
| **Model** | 模型选择下拉框 | `claude-sonnet-4-20250514` |
| **Reasoning** | 推理强度（影响思考深度） | `default` |

**Reasoning 推理强度选项**：`default`、`low`、`medium`、`high`、`max`

### OpenAI 设置

进入 **Settings → OpenAI** 页面配置 OpenAI API：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| **API Key** | OpenAI 的 API 密钥（以密码模式显示） | 无 |
| **Base URL** | API 基础地址，支持兼容 API 的第三方服务 | `https://api.openai.com` |
| **Model** | 模型选择下拉框 | `gpt-4o` |
| **Reasoning** | 推理强度（适用于支持推理的模型） | `default` |

**Reasoning 推理强度选项**：`default`、`low`、`medium`、`high`、`xhigh`

> **提示**：Base URL 支持填写任何 OpenAI 兼容的 API 地址，方便使用第三方中转服务或自建代理。
>
> **本地模型支持**：对于本地部署的 Ollama、LM Studio 等服务，将 Base URL 改为 `http://localhost:11434`（Ollama 示例），API Key 可留空，Model 字段支持直接输入（如 `llama3.2`、`qwen2.5` 等）。

### 通用设置

进入 **Settings → General** 页面配置全局参数：

#### System Prompt（系统提示词）

自定义 AI 的行为角色。在文本框中输入你希望 AI 遵循的指令，例如：
- "你是一个专业的编程助手，擅长 Python 和 C++。"
- "你是一个友善的聊天伙伴，用轻松幽默的方式回答问题。"

#### Parameters（参数）

| 参数 | 说明 | 范围 | 默认值 |
|------|------|------|--------|
| **Temperature** | 温度值，越高回复越随机创意，越低越确定保守 | 0.0 ~ 2.0 | 0.7 |
| **Max Tokens** | 单次回复的最大 token 数 | 100 ~ 128000 | 4096 |

#### Role Names（角色名称）

自定义聊天气泡上方显示的名称：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| **User Name** | 用户消息的显示名 | `You` |
| **Assistant Name** | 助手消息的显示名 | `Assistant` |

修改后即时生效，所有已有气泡的名称标签会同步更新。

#### Prompt Templates（Prompt 模板）

在文本框中每行输入一条 Prompt 模板，保存后会在 Chat 页面输入框上方显示为可点击的标签按钮。默认包含 6 个常用模板，可以增删改。

#### Tachie Character（立绘角色）

下拉框列出 `config/` 目录下所有包含 `config.ini` 的子目录，选择后立即切换为对应角色的立绘资源。

#### Background（背景图）

| 配置项 | 说明 |
|--------|------|
| **Paint Mode** | 背景绘制模式：Normal（纯色）/ Pixmap（静态图片）/ Movie（GIF 动画） |
| **Background Image** | 自定义静态背景图的本地路径（.png/.jpg），留空使用内置图片 |
| **Background Movie** | 自定义动态背景图的本地路径（.gif），留空使用内置动画 |

#### TTS (Text-to-Speech)

| 配置项 | 说明 |
|--------|------|
| **Enable TTS** | 开关，启用后 AI 每次回复会自动语音朗读 |
| **Voice** | 语音选择，支持多种中文、英文、日文语音 |

---

## 明/暗主题切换

点击标题栏右上角的主题切换按钮，可在亮色和暗色主题之间一键切换。所有界面元素（聊天气泡、设置页、输入框等）会自动适配。

---

## ASCII Art 工具页

导航栏中的 **ASCII Art** 页面可以把任意图片转换成 ASCII 字符画。

### 使用步骤

1. 点击 **Select Image** 按钮或直接拖拽图片到页面
2. 调整参数：
   - **Width** — 字符宽度（20~200，默认 80）
   - **Charset** — Simple（10 级字符）/ Detailed（70 级字符）
   - **Color** — 开关，彩色保留原图 RGB 颜色
3. 右侧实时预览 ASCII 效果

### 输出

| 按钮 | 输出 |
|------|------|
| **Copy** | 复制到剪贴板（彩色模式下同时复制富文本和纯文本） |
| **Save TXT** | 保存为 .txt 文件并自动打开 |
| **Save HTML** | 保存为完整 HTML 页面（带等宽字体和黑底） |
| **Save PNG** | 渲染为图片保存（所见即所得） |

> 聊天页中含图片附件的消息，右键菜单里也有 **Convert to ASCII** 选项，可将图片快速转为 ASCII 复制到剪贴板。

---

## Analytics 会话分析页

导航栏中的 **Analytics** 页面展示所有会话的统计数据：

- **Total Sessions** — 总会话数
- **Total Messages** — 总消息数
- **Estimated Tokens** — 估算 token 总数
- **Favorite Messages** — 收藏消息总数
- **Claude / OpenAI Sessions** — 按 Provider 分布
- **Most Active Session** — 消息最多的会话标题和消息数

每次打开页面时数据自动刷新。

---

## Compare 模型对比页

导航栏中的 **Compare** 页面可以将同一个问题同时发给 Claude 和 OpenAI，左右分栏显示两个模型的流式回复，方便对比质量。

使用方式：在底部输入框中输入问题 → 点击 **Send to Both** → 左侧显示 Claude 的回复，右侧显示 OpenAI 的回复。

> 对比模式不保存到会话记录，仅用于单次比较。

---

## 立绘表情联动

LLMChat 内置了一个独立的立绘（Tachie）窗口，会根据 AI 回复的情绪自动切换角色表情。

### 工作原理

1. 当立绘功能启用时，系统会在 System Prompt 末尾自动追加情绪标签指令
2. AI 每次回复会在开头加上如 `[高兴]`、`[正常]` 等情绪标签
3. 应用解析到标签后，自动触发立绘表情切换（带淡入淡出动画）
4. 情绪标签会从聊天气泡和历史记录中自动剥离，不影响阅读

### 可用表情

内置的 ATRI 立绘资源包含以下表情：

> 正常、高兴、伤心、生气、愤怒、兴奋、好奇、尴尬、惊呆、失落、哭泣、担心、自信、认真、鄙视、观望、睡觉、充满干劲、举手-开心、举手-吃惊、举手-生气、举手-认真、侧身-正常、侧身-高兴、侧身-兴奋

### 立绘窗口操作

- **拖拽移动**：按住左键拖动立绘窗口到任意位置
- **位置记忆**：窗口位置自动保存，下次启动恢复
- **右键菜单**：右键点击立绘窗口可重置位置或隐藏窗口
- **显示/隐藏**：快捷键 `Ctrl+T`

### 启用/禁用立绘

在配置文件 `config/settings.ini` 中，`[Tachie]` 分组下的 `enabled` 字段控制是否启用（默认启用）。

### 切换立绘角色

在 **Settings → General → Tachie Character** 下拉框中选择。系统自动扫描 `config/` 目录下所有包含 `config.ini` 的子目录作为候选。

### 嘴型同步

TTS 朗读 AI 回复期间，立绘会在几种表情（"说话"/"兴奋"/"高兴"/"正常"）之间切换模拟嘴部动作，播放结束后恢复到原情绪表情。立绘资源包需包含这些命名的表情才生效。

---

## TTS 语音朗读

LLMChat 集成了基于 Microsoft Edge 的 TTS 语音合成服务，可自动朗读 AI 的回复。

### 启用 TTS

1. 进入 **Settings → General** 页面
2. 找到 **TTS (Text-to-Speech)** 分区
3. 打开 **Enable TTS** 开关

### 可选语音

| 语言 | 语音示例 |
|------|---------|
| **中文** | zh-CN-XiaoxiaoNeural（晓晓）、zh-CN-YunxiNeural（云希）、zh-CN-XiaoyiNeural（晓伊）等 |
| **英文** | en-US-JennyNeural、en-US-GuyNeural、en-US-AriaNeural |
| **日文** | ja-JP-NanamiNeural、ja-JP-KeitaNeural |

### 使用说明

- 启用后，每当 AI 完成一条回复会自动语音合成并播放
- 发送新消息时会自动中止上一轮朗读
- TTS 启用后会预先建立 WebSocket 连接以减少首次合成延迟

---

## 背景图自定义

在 **Settings → General → Background** 中配置三种背景模式：

- **Normal** — 纯色背景
- **Pixmap** — 静态背景图（支持 .png/.jpg，留空使用内置图）
- **Movie** — 动态 GIF 背景（留空使用内置动画）

修改后即时生效，无需重启应用。

---

## 系统托盘

LLMChat 支持最小化到系统托盘后台常驻：

- **单击/双击托盘图标** — 显示或隐藏主窗口
- **右键托盘图标** — 弹出菜单：**show**（显示）/ **exit**（退出）

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 发送消息 |
| `Shift+Enter` | 输入框内换行 |
| `Esc` | 中止 AI 回复（保留已接收的部分内容） |
| `Ctrl+F` | 在当前会话中搜索消息 |
| `Ctrl+T` | 切换立绘窗口显示/隐藏 |
| `Ctrl+Alt+L` | 全局热键：从任意应用唤醒/隐藏主窗口 |
| `Ctrl + =` / `Ctrl + 滚轮上` | 放大聊天字体 |
| `Ctrl + -` / `Ctrl + 滚轮下` | 缩小聊天字体 |
| `Ctrl + 0` | 重置聊天字体为默认大小 |
| `Ctrl + V` | 粘贴剪贴板图片为附件 |

---

## 数据存储与备份

所有数据保存在可执行文件同级目录下的 `config/` 文件夹中：

```
config/
├── settings.ini          # 应用配置（API Key、模型、参数、角色名称等）
├── sessions/             # 聊天记录（每个会话一个 JSON 文件）
│   ├── {uuid1}.json
│   └── {uuid2}.json
└── ATRI0.3/              # 立绘资源
    ├── config.ini
    ├── anim.ini
    ├── 正常.png
    └── ...
```

**备份**：复制整个 `config/` 文件夹即可。

**迁移**：将 `config/` 文件夹复制到新机器的 LLMChat 同级目录下即可恢复所有数据。

---

## 常见问题

### Q: 发送消息后显示 "Error: ..."

请检查：API Key 是否正确、Base URL 是否可访问、网络是否正常、所选模型是否在你的 API 计划中可用。

### Q: 立绘窗口没有出现

确认 `config/settings.ini` 中 `[Tachie]` 下 `enabled=true`，或按 `Ctrl+T` 切换显示。确保 `config/ATRI0.3/` 目录存在。

### Q: TTS 语音没有播放

确认已在 Settings → General 中开启 Enable TTS、网络能正常访问微软语音服务、系统音频输出正常。

### Q: 如何使用第三方 API 代理？

在对应的设置页（Claude 或 OpenAI）中修改 **Base URL** 为你的代理地址即可。OpenAI 的 Base URL 支持任何兼容 API 地址。

### Q: 如何更换立绘角色？

将新的立绘资源（以情绪命名的 PNG 文件 + config.ini + anim.ini）放入 `config/` 下对应的目录，并确保目录名与程序中配置一致。
